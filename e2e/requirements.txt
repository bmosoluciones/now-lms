# Browser-E2E layer (L6) — deliberately OUTSIDE the hash-pinned test.lock:
# playwright wheels are platform-variant and the E2E layer is advisory until
# promoted per tests/TESTING.md policy. App deps still install from test.lock.
playwright==1.49.1
pytest==8.3.4
pytest-playwright==0.6.2
pytest-base-url==2.1.0
